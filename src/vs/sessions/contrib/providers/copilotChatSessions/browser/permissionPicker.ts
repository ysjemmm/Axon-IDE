/**
 * Stub: Copilot permission picker — Axon IDE does not ship Copilot.
 */

import { Disposable } from '../../../../../base/common/lifecycle.js';

export interface IPermissionPickerDelegate {
	// stub interface — no members required by our build
}

export class PermissionPicker extends Disposable {
	constructor() {
		super();
	}

	render(_container: HTMLElement): void {
		// no-op stub
	}
}
