/**
 * Stub: Copilot mobile permission picker — Axon IDE does not ship Copilot.
 */

import { Disposable } from '../../../../../base/common/lifecycle.js';
import type { IPermissionPickerDelegate } from './permissionPicker.js';

export class MobilePermissionPicker extends Disposable {
	constructor(_delegate: IPermissionPickerDelegate) {
		super();
	}

	render(_container: HTMLElement): void {
		// no-op stub
	}
}
